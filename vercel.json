{
  "version": 2,
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python"
    }
  ],
  "buildCommand": "pip3 install torch torchvision --index-url https://download.pytorch.org/whl/cpu && echo 'Dependencies installed!'",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ]
}
